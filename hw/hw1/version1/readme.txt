Homework 1. Due Monday, Jan 29.

Modify and enhance Staff-Employee project (Exercise P6.14 and below) as follows.

1. Prevent the user from adding an employee with a name that already exist.
   Use function find()in add_employee() to see whether the employee already exist.
2. In raise_salary() give the minimum of percentage computed and $1,000.00.
   Compute the raise, if it's greater than $1,000.00, then the raise would only be $1,000.00.
   
Hand in a print out of the complete source code and typescript of a sample run demonstrating clearly
that your program works correctly for all possible use cases.
